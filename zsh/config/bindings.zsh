# Keyboard bindings
# --------------------------------------
bindkey -v # Vim mode!
bindkey "^r" history-incremental-search-backward
bindkey "^w" history-incremental-search-forward
bindkey ' ' magic-space
